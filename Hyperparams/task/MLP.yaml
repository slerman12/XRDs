defaults:
  - classify@_global_: custom  # Custom classify
  - _self_

Dataset: XRD.XRD
Predictor: XRD.MLP

Aug: Identity
Eyes: Identity
Trunk: Identity

standardize: false
norm: true

num_classes: 7
dataset:
  num_classes: ${num_classes}

task_name: ${dataset.num_classes}-Way
experiment: NPCNN
